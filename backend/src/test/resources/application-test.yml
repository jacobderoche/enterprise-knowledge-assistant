spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

app:
  ai-service-base-url: http://localhost:9
  jwt-secret: test-secret-that-is-long-enough-for-hs256-signing-please-1234
  jwt-issuer-uri: ""
